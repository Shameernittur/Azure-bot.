name: My first workflow using actions

on: push

jobs: 
    First-job:
     runs-on: ubuntu-latest
     steps:
     - name: Welcome page
       run: echo "My first workflow using github actions"

     - name: List files
       run: ls
     
     - name: Read file
       run : cat README.md

     